# ── Backend Connection ──
# URL of the Express backend (API + Socket.io)
VITE_BACKEND_URL=http://localhost:3000

# ── Terminal WebSocket ──
# Host:port for the terminal WebSocket server (no protocol prefix)
VITE_TERMINAL_URL=localhost:4000
