FROM node:22

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source
COPY . .

# Expose port
EXPOSE 5173

# Start Vite dev server with host flag for Docker access
CMD ["npm", "run", "dev", "--", "--host"]
